extends base
include mixins/video

block content
    div.profile__data
        if (user.avatarUrl)
            img.avatar(src=user.avatarUrl)
        else
            img.avatar(src="https://mdtube.s3.ap-northeast-2.amazonaws.com/defaults/default_avatar.jpg")
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 업로드한 영상이 없습니다.